# Project preferences

- Commit on the user's behalf without assistant co-author trailers.
- Default to a fresh contract deployment when contract changes require deployment. Preserve earlier escrows, balances, manifests and bounty links; never assume a redeployment moves existing funds.
- Keep the static frontend on the existing GitHub Pages branch (`codex/pages`) with Cloudflare DNS for `issue.fund`.
